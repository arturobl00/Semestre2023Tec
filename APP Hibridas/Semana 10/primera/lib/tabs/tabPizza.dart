import 'package:flutter/material.dart';

class TabPizza extends StatelessWidget {
  const TabPizza({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("TabPizza");
  }
}